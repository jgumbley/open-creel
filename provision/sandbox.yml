- name: Provision sandbox layer
  hosts: localhost
  connection: local
  become: true
  gather_facts: true
  vars:
    gondolin_repo: "{{ gondolin_repo_url | default('https://github.com/earendil-works/gondolin.git') }}"
  roles:
    - sandbox
